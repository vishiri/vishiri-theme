# Vishiri Theme

This extension contains the `Vishiri Theme` VS Code color theme.

## Palette Remap

To re-apply the current gold, silver, and crimson palette preset:

```powershell
.\scripts\Apply-ThemePalette.ps1 -ThemePath '.\themes\Vishiri Theme-color-theme.json'
```

